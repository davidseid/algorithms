const countAndSay = (n) => {
  // initiate the first term n === 1 return 1; 
  // for each in n, update the result, at the end return the value;

  let result = "1";
  if (n === 1) return result; 

  let num = result[0];
  let count = 0;
  let index = 0;
  let nextResult = '';

  while (result[index] === num) {
    
  }
    

}

console.log(countAndSay(4));